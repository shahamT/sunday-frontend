// === Libs


// === Services

// === Actions

// === Hooks / React

// === Imgs

// === Child Components

// ====== Component ======
// =======================

export function T_GroupFooter({ /* prop1, prop2 */ }) {
    // === Consts

    // === Effects

    // === Functions

    // if (!data) return <div>Loading...</div>
    return (
        <section className="T_GroupFooter">
            <div className="menu-wraper" />
            <div className="t-left-indicator bottom" />
            <div className="row-wraper t-row footer">
                <input type="checkbox" name="" id="" />
                <p>group footer</p>
            </div>
        </section>
    )
}