// === Libs


// === Services

// === Actions

// === Hooks / React

// === Imgs

// === Child Components
import { T_Group } from "./T_Group";

// ====== Component ======
// =======================

export function T_GroupsList({ board }) {
    // === Consts
    // === Effects

    // === Functions

    // if (!data) return <div>Loading...</div>
    return (
        <section className="T_GroupsList">

        </section>
    )
}